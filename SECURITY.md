# Security Policy

## Supported versions

安全修复只保证进入最新版本。发现问题后，请先停用脚本，并确认安装来源是否为官方仓库或正式 Release。

## Reporting a vulnerability

请通过 GitHub Security Advisory 私下报告安全问题，不要先创建公开 Issue。报告建议包含：

- 受影响版本和浏览器版本；
- 可复现的最小步骤；
- 实际影响和预期行为；
- 如涉及页面内容，先移除 Cookie、令牌、私信及其他个人信息。

维护者应在确认问题后尽快发布修复版本，并在用户有明确更新路径后再披露细节。

## Security boundaries

本项目只负责楼中楼页面展示。脚本显式发起的网络请求仅限同源只读 GET：同一帖子的分页 HTML 使用 `credentials: 'same-origin'`。请求必须具备同帖子路径白名单、响应大小、超时、页数和并发限制。脚本不得自行请求个人资料或调用任何账号操作 API。

置顶/HOT 评论在普通会话中的镜像不会自行执行账号操作。只有用户亲自点击菜单时，操作才交给 NodeSeek 原生控件。跨页紧凑正文来自经清理的惰性 HTML；第一次展开详情时，才允许加载严格校验过的同一帖子来源页，并复用 NodeSeek 已挂载完成的原生评论节点。来源页按页缓存，加载超时后必须移除并允许重试。

原生预加载 iframe 必须同时满足：仅当前域名、当前帖子、12 页上限；`sandbox` 只能包含 `allow-scripts allow-same-origin`；不得加入表单、弹窗、下载、顶层跳转等权限；不得使用 `srcdoc` 或页面内容提供的任意 URL。iframe 仅用于用户主动展开过的来源页，不能在首屏批量执行所有分页脚本。页面自身加载的站点子资源属于 NodeSeek 原页面行为，脚本不得额外添加第三方地址。

点赞、鸡腿和反对等操作继续由被复用的 NodeSeek 菜单组件处理。回复和引用必须调用当前页面的 NodeSeek 原生编辑器；来源页配置只能在同步调用期间临时替换 `window.__config__`，并在 `finally` 中恢复。个人卡片必须调用 NodeSeek 自带 `hoverCard` 组件，不得自行获取资料或绘制替代卡片。若原生组件无法确认，功能必须失败关闭，不能猜测接口。

从来源 iframe 搬入的原生楼层及其子节点必须使用结构化 DOM 特征校验，不能用当前窗口的 `HTMLElement`、`HTMLAnchorElement` 等构造器做跨窗口 `instanceof` 判断；否则合法节点会被误判并在重新归组时丢失。

项目不得增加账号自动化、跨域请求、第三方代码地址、Cookie 读取、无关存储读取、跨窗口消息接口或绕过站点权限的功能。唯一允许的持久化数据是固定键 `ns-nested-replies:enabled` 下的布尔开关。若未来确需增加权限，必须单独说明威胁模型、权限理由和数据流，并在合并前进行安全审查。
